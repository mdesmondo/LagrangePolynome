# Интерполяция функции полиномом Лагранжа

Лабораторная работа 1 по курсу "Вычислительная математика" в СПбПУ (Политех).

## 📋 Описание задачи

Реализация интерполяции функции f(x) = (3+x)^|x| · sin(2x) + 1 на отрезке [-1, 2] с использованием интерполяционного полинома Лагранжа. Исследование зависимости точности приближения от степени полинома и сравнение фактической погрешности с теоретической оценкой.

## 🎯 Цель работы

Изучение и практическое применение:

- Метода интерполяции Лагранжа
- Построения базисных полиномов Лагранжа
- Оценки погрешности интерполяции через производные высших порядков
- Численного дифференцирования с помощью библиотеки numdifftools
- Визуализации результатов интерполяции

## 📐 Математическая основа

**Интерполяционный полином Лагранжа степени m:**

$$Q_m(x) = \sum_{i=0}^{m} f(x_i) \cdot l_i(x)$$

где базисные полиномы:

$$l_i(x) = \prod_{j=0, j \neq i}^{m} \frac{x - x_j}{x_i - x_j}$$

**Оценка погрешности интерполяции:**

$$|f(x) - Q_m(x)| \leq \frac{M_{m+1}}{(m+1)!} \cdot |\omega(x)|$$

где $M_{m+1} = \max |f^{(m+1)}(x)|$, $\omega(x) = \prod_{i=0}^{m}(x - x_i)$

## 🛠 Технологии

- Язык: Python 3
- Среда: JupyterLab
- Библиотеки:
  - `numpy` — работа с массивами и математические операции
  - `matplotlib` — визуализация графиков
  - `scipy` — специальные функции (факториал)
  - `numdifftools` — численное дифференцирование

## 🚀 Функциональность программы

### Основные компоненты

1. **Функция f(x)** — исходная интерполируемая функция
2. **Функция Q(x, m, nodes)** — вычисление интерполяционного полинома Лагранжа и произведения ω(x)

### Процесс исследования

Программа выполняет анализ для степеней полинома **m = 3, 5, 7, 9**:

1. **Построение узлов интерполяции** — равномерная сетка из m+1 точек
2. **Вычисление полинома Лагранжа** — значения в 1000 точках отрезка
3. **Визуализация результатов** — графики функции, полинома и узлов интерполяции
4. **Анализ фактической ошибки** — вычисление максимального отклонения
5. **Теоретическая оценка погрешности** — расчёт через производную (m+1)-го порядка
6. **Сравнительный анализ** — графики фактической и теоретической ошибок

### Выходные данные

Для каждой степени m:
- График интерполяционного полинома и исходной функции
- График модуля производной (m+1)-го порядка
- График сравнения фактической ошибки с теоретической оценкой
- Численные значения максимумов производной и ошибки

Итоговые графики:
- Зависимость максимальной фактической ошибки от степени m
- Зависимость максимальной теоретической оценки от степени m

## 💡 Ключевые результаты

**Влияние степени полинома:**
- С увеличением степени полинома точность интерполяции может как улучшаться, так и ухудшаться
- При использовании равномерной сетки узлов возможно проявление феномена Рунге (рост ошибки на краях отрезка)

**Теоретическая оценка:**
- Верхняя граница погрешности через производную высшего порядка
- Сравнение показывает, насколько точна теоретическая оценка для данной функции

## 🚀 Установка и запуск

### Требования

- Python 3.8+
- JupyterLab

## 👤 Автор

Михаил Карпенко  
Санкт-Петербургский политехнический университет Петра Великого  
2025

## 📚 Курс

Вычислительная математика 
Институт компьютерных наук и кибербезопасности
